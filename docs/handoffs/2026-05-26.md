# 2026-05-26 — socialisn → socialisn2 migration complete

Goal this session: **replace socialisn (v1) with socialisn2** — decommission v1, and
give Claude first-class access to socialisn2 (incl. claude.ai web). Done.

## START HERE NEXT SESSION

Two carry-overs, both non-blocking (Build tasks `QXUzejM4d2JZRWEzY1M5Xw` +
`Y1pITC1kU2piT0daYUJCTg`):
1. **Upload the repointed skill:** `/Users/leesimon/tmp/episode-prep-socialisn2.skill` →
   claude.ai → Settings → Capabilities → Skills (replaces the v1 `episode-prep`). [user action]
2. **First clean autonomous orchestrator run** still pending observation (the
   single-source over-merge / ranking-source-diversity question from 2026-05-25.md).

## What shipped

### socialisn v1 — fully decommissioned
- `unsubject/socialisn` repo **archived**; `unsubject/frontierwatch2` confirmed never existed as a repo (it was n8n-only).
- **18 n8n workflows unpublished** (10 socialisn-core + 8 FrontierWatch2). Telegram Summarizer Bot (`OhGHX0F3xobHAqHc`) left running — unrelated.
- **Railway Postgres deleted** by Simon (data + DB + billing; no backup, his call). It held socialisn-core + `frontier_*` (FrontierWatch2) + `studio_*` (socialisn-studio v1) — all gone.
- Claude footprints removed: `railway-pg` MCP dropped from `~/.claude.json`; `~/.claude/CLAUDE.md` tag map → `socialisn2 → #socialisn2`; auto-memory tombstoned.

### socialisn2 — now reachable on all three Claude surfaces
- **Claude Code** + **Desktop**: unchanged — `https://mcp.socialisn.com/mcp` + static bearer (Desktop via `mcp-remote` in `claude_desktop_config.json`).
- **claude.ai web (NEW): OAuth gateway.** `oauth-worker/` (`socialisn2-oauth`, `@cloudflare/workers-oauth-provider@0.7.0`) at **`mcp-oauth.socialisn.com`** — GitHub-OAuth-gates consent to login `unsubject`, proxies authenticated `/mcp` to the VPS injecting the bearer. **Verified live:** 401+`WWW-Authenticate`, RFC 9728 PRM, RFC 8414 AS metadata, PKCE S256. **MCP connector connected in claude.ai web.**
- **`mcp.socialisn.com` untouched** — still serves `/mcp` (bearer), `/status` (ops-digest), `/c/:id` (Telegram links), `/feeds`.

### Key design pivot (ADR Option B → "own subdomain")
The approved ADR moved `mcp.socialisn.com` wholesale to the Worker — that would have **broken `/status`, `/c/:id`, `/feeds`** (the Worker only handles `/mcp`+OAuth). Caught before any damage; switched to: gateway on its **own subdomain**, origin host unchanged, no DNS cutover, no Code/Desktop repoint, `mcp-origin` not needed. See [[cf-worker-oauth-gateway-for-mcp]] memory for the reusable recipe + gotchas.

### episode-prep skill — repointed
The claude.ai Skill called dead socialisn-studio v1 tools. Rewritten to socialisn2's MCP:
- `list_daily_candidates`→`list_candidates`; `search_discourse`→`search_candidates`+`get_candidate.sources[]`; **`build_thesis_brief` (no socialisn2 equiv) → Claude synthesises the brief** from candidate detail + `compare_against_archive`.
- New: Phase 2 calls `pick`/`pass` to feed preference learning.
- **Domain-taxonomy correction (verified against live data):** socialisn2 domains are TOPICAL (economy/economics/scitech/geopolitics/national), NOT geographic — `national` = domestic news from many countries (HK/UK/NZ/Taiwan), HK stories span ≥3 domains. So the HK pass uses `search_candidates(query="Hong Kong 香港")`, not a `domain` filter.

## CI / infra changes on main
- `inspect-pg.yml` (read-only SQL) — merged 2026-05-25.
- `deploy-oauth-worker.yml` (workflow_dispatch; deploys oauth-worker + pushes Worker secrets from GH repo secrets `SOCIALISN2_MCP_TOKEN`/`OAUTH_GH_CLIENT_ID`/`OAUTH_GH_CLIENT_SECRET`/`OAUTH_COOKIE_KEY`).
- Root `vitest.config.ts` excludes `oauth-worker/**` (separate Workers-runtime test package).
- CF: KV namespace `socialisn2-oauth-OAUTH_KV` (`81ec45bb…`), account Simon Lee `2f58eaae…`.

## Decisions / gotchas worth keeping
1. **Gateway on own subdomain**, not a cutover — protects the shared `mcp.socialisn.com` surface.
2. **CF token lacked zone Workers-Routes:Edit** (`auth 10000` on `/workers/routes`) → custom domain bound via **CF dashboard** instead of wrangler `custom_domain` (persists across deploys).
3. **GH secret names can't start with `GITHUB_`** → `OAUTH_GH_CLIENT_ID/SECRET` mapped to Worker `GITHUB_CLIENT_ID/SECRET` in the deploy workflow.
4. **MCP token was exposed in this transcript** (a `claude mcp get` printed the header); Simon **accepted the risk**, no rotation.
5. Unverified (confirm next live web call): claude.ai sends RFC 8707 `resource` so the gateway audience-binds tokens.

## Branch state
`main` at the handoff commit. No open PRs (#96/#97/#98/#99 merged). `oauth-worker` deployed manually via `deploy-oauth-worker.yml`; custom domain via dashboard. `mcp.socialisn.com` + Code + Desktop unchanged.
