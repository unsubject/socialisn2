// Phase 0 stub. Replaced by the real Fastify entrypoint per SPEC §16 in a
// later phase. Exists so `tsc --noEmit` has at least one input file and so
// the Dockerfile's build stage produces a non-empty dist/.

export {};
